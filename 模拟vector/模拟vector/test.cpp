#include"test.h"

int main()
{
	lcx::test1();
	/*vector<int> v;
	v.reserve(12);
	v.push_back(1);
	v.push_back(1);
	v.push_back(1);
	v.push_back(1);
	cout << v.capacity() << endl;
	for (auto e : v)
	{
		cout << e <<" ";
	}
	cout << endl;

	v.resize(10,2);
	cout << v.capacity() << endl;
	for (auto e : v)
	{
		cout << e << " ";
	}
	cout << endl;*/
	system("pause");
	return 0;
}